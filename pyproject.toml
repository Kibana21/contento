[project]
name = "aia-marketing-studio"
version = "0.1.0"
description = "Governed AI creative workspace for AIA Singapore representatives"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9",
    "pydantic-ai-slim[google]>=2.0",
    "google-genai",
    "pyyaml",
    "jinja2",
    "segno",
    "pillow",
    "textstat",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-cov", "pytest-asyncio"]
# The free-form engine runs without this; the graph is a thin optional shell.
graph = ["langgraph"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"

[tool.ruff]
line-length = 110
target-version = "py312"
